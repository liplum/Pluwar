import 'package:flutter/material.dart';

class ActionInformation extends StatelessWidget {
  const ActionInformation({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
